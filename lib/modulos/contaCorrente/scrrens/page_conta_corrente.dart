import 'package:flutter/material.dart';

class PageContaCorrente extends StatefulWidget {

  const PageContaCorrente({ super.key });

  @override
  State<PageContaCorrente> createState() => _PageContaCorrenteState();
}

class _PageContaCorrenteState extends State<PageContaCorrente> {

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}