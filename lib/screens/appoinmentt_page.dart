import 'package:flutter/material.dart';

class AppoinmenttPage extends StatelessWidget {

  const AppoinmenttPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Center(
        child: Text('Apoinment Page')
        );
  }
}